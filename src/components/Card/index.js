import react from 'react'

function Card(prop) {
    return (
        <div>
            <img className="" src={props.item.imgSrc} alt={props.item.imgSrc} />
        </div>
    )
}

export default Card;